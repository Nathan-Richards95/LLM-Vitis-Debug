#include <ap_int.h>

#define MAX_ITERATIONS 1024

int main() {
#pragma HLS ARRAY_PARTITION variable=ap_int<32> type=cyclic dim=1 factor=1
    int i = 0;
    for (i = 0; i < MAX_ITERATIONS; i++) {
#pragma HLS PIPELINE OFF
#pragma HLS UNROLL factor=16
        ap_int<32> a = i;
    }
}